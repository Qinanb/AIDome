# ============================================================
# Nanoclaw 项目依赖
# 安装方式: pip install -r requirements.txt
# ============================================================

# --- LLM API 客户端 (OpenAI 兼容接口) ---
openai>=1.90.0

# --- Model Context Protocol (MCP 客户端 + FastMCP 服务端) ---
mcp>=1.27.0

# --- Web 频道 (HTTP/WebSocket) ---
fastapi>=0.100.0
uvicorn>=0.30.0

# --- 飞书/Lark 消息频道 ---
lark-oapi>=1.6.0

# --- QQ 机器人消息频道 ---
qq-botpy>=1.2.0

# --- YAML 配置解析 (skills 模块) ---
PyYAML>=6.0

# --- 可选: MCP 子进程服务器 (config.json 中引用) ---
# mcp-server-sqlite          # SQLite MCP 服务 (python -m mcp_server_sqlite)
# 注意: @modelcontextprotocol/server-filesystem 是 npm 包，需通过 npm 安装
